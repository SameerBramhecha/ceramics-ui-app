# Contracts

This feature does not introduce external runtime APIs or cross-repository contracts. The design system is documented for use by the site's frontend components. If contracts are later required (e.g., a token package or component library), this directory will contain the contract definitions.
